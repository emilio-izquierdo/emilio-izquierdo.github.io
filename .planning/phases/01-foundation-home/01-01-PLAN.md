---
phase: 01-foundation-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .nojekyll
  - assets/css/style.css
  - components/nav.html
  - assets/js/nav.js
  - index.html
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - HOME-01
  - HOME-02
  - HOME-03

must_haves:
  truths:
    - "Opening http://localhost:8000 in a browser shows a styled home page with name, role, bio, and email"
    - "Navigation bar appears with 5 links: Home, Research, Projects, Writing, CV"
    - "Email address is a clickable mailto link"
    - "Page is readable on a 375px-wide viewport without horizontal scroll"
  artifacts:
    - path: ".nojekyll"
      provides: "Disables Jekyll processing on GitHub Pages"
    - path: "assets/css/style.css"
      provides: "Complete site stylesheet with reset, tokens, typography, layout, nav, home, responsive"
      min_lines: 100
    - path: "components/nav.html"
      provides: "Shared navigation partial with 5 links"
      contains: "aria-label"
    - path: "assets/js/nav.js"
      provides: "Fetch-inject nav loader with aria-current active state"
      contains: "aria-current"
    - path: "index.html"
      provides: "Home page with name, role, bio, email, and section links"
      contains: "mailto:"
  key_links:
    - from: "index.html"
      to: "/assets/css/style.css"
      via: "link rel=stylesheet in head"
      pattern: "href=./assets/css/style.css."
    - from: "index.html"
      to: "/assets/js/nav.js"
      via: "script src at end of body"
      pattern: "src=./assets/js/nav.js."
    - from: "assets/js/nav.js"
      to: "/components/nav.html"
      via: "fetch call"
      pattern: "fetch.*/components/nav.html."
---

## Phase Goal

**As a** PI reviewing applications, **I want to** visit a live personal website that shows the researcher's name, bio, and email with navigation to all sections, **so that** I can quickly understand who this researcher is and how to reach them.

<objective>
Build the complete shared infrastructure (CSS, nav partial, nav loader) and the home page -- the first vertical slice. After this plan, running a local HTTP server from the repo root shows a fully styled, functional home page with working navigation. This covers FOUND-02 (.nojekyll), FOUND-03 (shared nav infrastructure), FOUND-04 (responsive layout), HOME-01 (name/role/bio), HOME-02 (email mailto), and HOME-03 (nav links to all 5 sections).

Purpose: Establish every shared file that all subsequent pages will use, and deliver the highest-priority page (home) as a working vertical slice.
Output: .nojekyll, style.css, nav.html, nav.js, index.html -- a locally testable site.
</objective>

<execution_context>
@/Users/emilio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emilio/allAbtMe/research-website/.planning/PROJECT.md
@/Users/emilio/allAbtMe/research-website/.planning/ROADMAP.md
@/Users/emilio/allAbtMe/research-website/.planning/STATE.md
@/Users/emilio/allAbtMe/research-website/.planning/phases/01-foundation-home/01-RESEARCH.md
@/Users/emilio/allAbtMe/research-website/.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared infrastructure files (.nojekyll, style.css, nav.html, nav.js)</name>
  <files>.nojekyll, assets/css/style.css, components/nav.html, assets/js/nav.js</files>

  <read_first>
    .planning/phases/01-foundation-home/01-RESEARCH.md (Pattern 2: nav.html markup, Pattern 3: nav.js code, Pattern 4: CSS structure with all 8 sections, Josh Comeau reset, design tokens, content wrapper, active nav CSS, responsive media query)
    .planning/research/STACK.md (font stacks, color palette, layout values, CSS reset)
  </read_first>

  <action>
    Create the directory structure and four infrastructure files:

    1. `.nojekyll` -- empty file at repo root (per FOUND-02). Zero bytes. Just touch the file.

    2. `assets/css/style.css` -- the complete site stylesheet following the 8-section structure from RESEARCH.md Pattern 4:
       - Section 1 DESIGN TOKENS: Copy the exact `:root` block from RESEARCH.md (colors: --color-bg #fafafa, --color-surface #ffffff, --color-text #1a1a1a, --color-text-muted #555555, --color-border #e0e0e0, --color-accent #1a56a0, --color-accent-visited #6b3fa0; fonts: --font-body Charter/'Bitstream Charter'/'Sitka Text'/Cambria/serif, --font-ui system-ui/-apple-system/'Segoe UI'/Roboto/Helvetica/Arial/sans-serif; type scale: --text-xs through --text-3xl; line heights: --leading-tight 1.2, --leading-normal 1.5, --leading-loose 1.7; layout: --max-width 680px, --padding-inline 1.5rem, --spacing-base 1.5rem)
       - Section 2 RESET: Copy the Josh Comeau 2025 reset verbatim from RESEARCH.md (10 rules: box-sizing, margin, body line-height + font-smoothing, media elements, form inheritance, overflow-wrap, text-wrap pretty on p, text-wrap balance on h1-h3)
       - Section 3 BASE TYPOGRAPHY: body uses var(--font-body) at var(--text-base) with var(--leading-loose) and var(--color-text) on var(--color-bg). Headings use var(--leading-tight). Links use var(--color-accent) with underline, visited use var(--color-accent-visited).
       - Section 4 LAYOUT: body min-height 100vh. `.content` class uses max-width var(--max-width), width 100%, margin-inline auto, padding-inline var(--padding-inline) -- exactly as in RESEARCH.md content wrapper pattern.
       - Section 5 NAVIGATION: `.site-nav` uses var(--font-ui). Display flex with justify-content space-between and align-items center. Wrap the nav in max-width var(--max-width) with margin-inline auto and padding var(--spacing-base) var(--padding-inline). Nav links are unstyled list items displayed inline with gap. `.nav-name` is bold, no underline. `a[aria-current="page"]` gets font-weight 600 and border-bottom 2px solid currentColor.
       - Section 6 HOME PAGE: `.home-hero h1` uses var(--text-3xl). `.subtitle` uses var(--text-lg) and var(--color-text-muted). `.bio` uses var(--text-lg) and var(--leading-loose) with margin-top var(--spacing-base). `.contact` margin-top var(--spacing-base). `.home-links` margin-top calc(var(--spacing-base) * 2), list-style none, display flex with gap.
       - Section 7 RESPONSIVE: Single media query at max-width 600px. Nav stacks vertically (flex-direction column, gap 0.5rem on .site-nav and .site-nav ul). Home links stack vertically. Adjust text sizes down slightly (--text-3xl to 1.75rem).
       - Section 8 UTILITIES: Leave a placeholder comment section for future utility classes.

    3. `components/nav.html` -- nav partial with NO html/head/body wrapper tags. Contains a `nav` element with class `site-nav` and `aria-label="Main navigation"`. Inside: an anchor with class `nav-name` linking to `/index.html` with text "Emilio" (placeholder name -- the user will update with their real name). Then a `ul` with 5 `li` items linking to `/index.html` (Home), `/research.html` (Research), `/projects.html` (Projects), `/writing.html` (Writing), `/cv.html` (CV). All paths root-relative.

    4. `assets/js/nav.js` -- the fetch-inject loader from RESEARCH.md Pattern 3. IIFE that gets `document.getElementById('site-nav')`, returns early if not found, fetches `/components/nav.html`, injects via innerHTML, then loops through all anchor elements in the placeholder to set `aria-current="page"` on the link whose `href` matches `window.location.href`. Use `function` syntax (not arrow functions) for maximum browser compatibility as shown in the research.
  </action>

  <verify>
    <automated>test -f .nojekyll && test -f assets/css/style.css && test -f components/nav.html && test -f assets/js/nav.js && grep -q "aria-label" components/nav.html && grep -q "aria-current" assets/js/nav.js && grep -q -- "--color-accent" assets/css/style.css && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - .nojekyll exists at repo root and is 0 bytes
    - assets/css/style.css contains all 8 section comment headers: "1. DESIGN TOKENS", "2. RESET", "3. BASE TYPOGRAPHY", "4. LAYOUT", "5. NAVIGATION", "6. HOME PAGE", "7. RESPONSIVE", "8. UTILITIES"
    - style.css :root block contains exactly these custom property names: --color-bg, --color-surface, --color-text, --color-text-muted, --color-border, --color-accent, --color-accent-visited, --font-body, --font-ui, --text-xs, --text-sm, --text-base, --text-lg, --text-xl, --text-2xl, --text-3xl, --leading-tight, --leading-normal, --leading-loose, --max-width, --padding-inline, --spacing-base
    - style.css contains the Josh Comeau reset rule "*, *::before, *::after { box-sizing: border-box; }"
    - style.css contains `.content` class with `max-width: var(--max-width)` and `margin-inline: auto`
    - style.css contains `a[aria-current="page"]` selector with `font-weight: 600`
    - style.css contains `@media (max-width: 600px)` responsive breakpoint
    - components/nav.html contains a `nav` element with `class="site-nav"` and `aria-label="Main navigation"`
    - components/nav.html contains exactly 5 `li` elements with links to index.html, research.html, projects.html, writing.html, cv.html
    - components/nav.html contains NO `<!DOCTYPE>`, `<html>`, `<head>`, or `<body>` tags
    - assets/js/nav.js contains `document.getElementById('site-nav')`
    - assets/js/nav.js contains `fetch('/components/nav.html')`
    - assets/js/nav.js contains `setAttribute('aria-current', 'page')`
  </acceptance_criteria>

  <done>All four infrastructure files exist with correct content. style.css has all 8 sections with design tokens, reset, typography, layout, nav, home, responsive, and utilities. nav.html is a valid partial with 5 nav links and aria-label. nav.js fetches and injects nav with active page detection.</done>
</task>

<task type="auto">
  <name>Task 2: Create index.html home page with real content structure</name>
  <files>index.html</files>

  <read_first>
    .planning/phases/01-foundation-home/01-RESEARCH.md (Pattern 1: HTML page shell, Pattern 5: home page content structure)
    components/nav.html (to verify nav-name text matches h1 name)
    assets/css/style.css (to verify class names match: content, home-hero, subtitle, bio, contact, home-links)
  </read_first>

  <action>
    Create `index.html` at the repo root using the exact HTML shell from RESEARCH.md Pattern 1, with home page content from Pattern 5.

    Head section:
    - `lang="en"` on the html element
    - `meta charset="UTF-8"`
    - `meta name="viewport" content="width=device-width, initial-scale=1.0"`
    - `title` set to "Emilio [Surname] | Researcher" (placeholder -- user will fill real name)
    - `meta name="description"` with content "Personal website of Emilio [Surname] -- researcher in [field]. Home, Research, Projects, Writing, and CV."
    - `link rel="stylesheet" href="/assets/css/style.css"` (root-relative path)

    Body section:
    - `div id="site-nav"` (empty -- nav.js injects nav here)
    - `main class="content"` containing:
      - `section class="home-hero"` with:
        - `h1` with researcher name (placeholder: "Emilio [Surname]")
        - `p class="subtitle"` with role/title and institution (placeholder: "Lab Manager / Research Specialist -- [Institution]")
        - `p class="bio"` with a 2-4 sentence bio placeholder written in active voice, first person, plain language. NOT "lorem ipsum" -- write a structural placeholder that the user can replace sentence by sentence. Example structure: "I study [research area] at [institution]. My work combines [method/approach] to understand [question]. Before this, I [relevant background]. I am currently looking for [what they seek]."
        - `p class="contact"` with `a href="mailto:email@example.com"` displaying "email@example.com" (placeholder the user will replace)
      - `nav class="home-links" aria-label="Site sections"` with a `ul` containing 4 `li` elements linking to research.html, projects.html, writing.html, cv.html (NOT index.html -- already on home)
    - `script src="/assets/js/nav.js"` at end of body (root-relative path)

    Do NOT use relative paths (no `../`). All asset paths must start with `/`.
  </action>

  <verify>
    <automated>test -f index.html && grep -q 'lang="en"' index.html && grep -q 'viewport' index.html && grep -q 'href="/assets/css/style.css"' index.html && grep -q 'id="site-nav"' index.html && grep -q 'class="home-hero"' index.html && grep -q 'mailto:' index.html && grep -q 'src="/assets/js/nav.js"' index.html && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - index.html exists at repo root
    - html element has `lang="en"`
    - head contains `meta name="viewport" content="width=device-width, initial-scale=1.0"`
    - head contains `meta name="description"` with researcher name
    - head contains `link rel="stylesheet" href="/assets/css/style.css"` (root-relative, not relative)
    - body contains `div id="site-nav"` before main
    - body contains `main class="content"`
    - main contains `section class="home-hero"` with h1, p.subtitle, p.bio, p.contact
    - p.contact contains `a href="mailto:email@example.com"` (or similar placeholder email)
    - body contains `nav class="home-links"` with `aria-label="Site sections"` and 4 links to research/projects/writing/cv
    - body ends with `script src="/assets/js/nav.js"` before closing body tag
    - NO relative paths (`../`) appear anywhere in the file -- all paths start with `/`
    - NO `<!DOCTYPE html>` is missing -- it must be present
    - Bio text is in active voice, first person -- NOT passive academic register, NOT lorem ipsum
  </acceptance_criteria>

  <done>index.html exists with correct HTML shell, viewport meta, root-relative asset paths, real content structure (name in h1, role in subtitle, active-voice bio placeholder, clickable mailto email), home-links nav to all 4 other sections, and nav.js script loaded at body end. Opening http://localhost:8000 after `python3 -m http.server 8000` shows the styled home page with injected navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to GitHub Pages CDN | All resources are static files served over HTTPS; no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Repo contents | accept | Repo is intentionally public; no secrets, no PII beyond the researcher's own public contact info |
| T-01-02 | Tampering | nav.js fetch | accept | Nav is fetched from same origin (GitHub Pages CDN); no third-party scripts; HTTPS provided automatically |
| T-01-03 | Denial of Service | GitHub Pages CDN | accept | GitHub Pages has its own rate limiting; out of project control; low-value target |
</threat_model>

<verification>
After both tasks complete:
1. From repo root, run `python3 -m http.server 8000` and open http://localhost:8000
2. Verify: styled home page loads with name, subtitle, bio, email visible
3. Verify: nav bar appears with 5 links (Home, Research, Projects, Writing, CV)
4. Verify: email is a clickable mailto link
5. Verify: browser DevTools responsive mode at 375px width shows no horizontal scroll, text is readable, nav stacks vertically
</verification>

<success_criteria>
- All 5 files exist (.nojekyll, style.css, nav.html, nav.js, index.html)
- Local server at http://localhost:8000 renders a styled page with visible name, role, bio, email
- Nav bar injects correctly with 5 links
- Page is responsive at 375px width
- No console errors in browser DevTools
</success_criteria>

<output>
Create `.planning/phases/01-foundation-home/01-01-SUMMARY.md` when done
</output>
