---
phase: 01-foundation-home
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - research.html
  - projects.html
  - writing.html
  - cv.html
  - 404.html
autonomous: false
requirements:
  - FOUND-01
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "All five nav links resolve to real pages without 404 errors locally"
    - "Each stub page shows the shared nav and a page title"
    - "The site is live at the GitHub Pages URL"
    - "HTTPS is active with no mixed content warnings"
  artifacts:
    - path: "research.html"
      provides: "Research stub page with nav and page title"
      contains: "site-nav"
    - path: "projects.html"
      provides: "Projects stub page with nav and page title"
      contains: "site-nav"
    - path: "writing.html"
      provides: "Writing stub page with nav and page title"
      contains: "site-nav"
    - path: "cv.html"
      provides: "CV stub page with nav and page title"
      contains: "site-nav"
    - path: "404.html"
      provides: "Custom 404 page with nav and return-home link"
      contains: "site-nav"
  key_links:
    - from: "research.html"
      to: "/assets/css/style.css"
      via: "link rel=stylesheet"
      pattern: "href=./assets/css/style.css."
    - from: "research.html"
      to: "/assets/js/nav.js"
      via: "script src"
      pattern: "src=./assets/js/nav.js."
---

## Phase Goal

**As a** PI reviewing applications, **I want to** visit a live personal website that shows the researcher's name, bio, and email with navigation to all sections, **so that** I can quickly understand who this researcher is and how to reach them.

<objective>
Create the four stub pages (Research, Projects, Writing, CV) plus a custom 404 page, then deploy to GitHub Pages and verify the live URL. After this plan, the site is live and shareable. This covers FOUND-01 (correct repo name and live URL), FOUND-03 (shared nav working across all 5 pages), and FOUND-04 (responsive layout verified on all pages).

Purpose: Complete the Walking Skeleton -- every nav link resolves, every page loads, the site is deployed and live.
Output: 4 stub pages, 404.html, live site at username.github.io.
</objective>

<execution_context>
@/Users/emilio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/emilio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/emilio/allAbtMe/research-website/.planning/PROJECT.md
@/Users/emilio/allAbtMe/research-website/.planning/ROADMAP.md
@/Users/emilio/allAbtMe/research-website/.planning/STATE.md
@/Users/emilio/allAbtMe/research-website/.planning/phases/01-foundation-home/01-RESEARCH.md
@/Users/emilio/allAbtMe/research-website/.planning/phases/01-foundation-home/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stub pages and custom 404</name>
  <files>research.html, projects.html, writing.html, cv.html, 404.html</files>

  <read_first>
    index.html (copy the HTML shell structure -- same head, same div#site-nav, same script tag at end; only change title, meta description, and main content)
    assets/css/style.css (verify .content class exists for the main element)
    components/nav.html (verify nav links match the filenames being created)
  </read_first>

  <action>
    Create 5 HTML files by copying the shell structure from index.html. Each page has:
    - Identical head structure (charset, viewport, stylesheet link to `/assets/css/style.css`)
    - Unique `title` tag: "[Page Name] | Emilio [Surname]"
    - Unique `meta name="description"` with page-specific content
    - `div id="site-nav"` (empty, nav.js injects here)
    - `main class="content"` with page-specific content
    - `script src="/assets/js/nav.js"` at end of body

    Page-specific content for each stub:

    research.html:
    - `h1` with text "Research"
    - One `p` element with intentional framing copy: "Details about research interests and past work are on their way. Check back soon." Do NOT use "under construction" language -- frame as active intent.

    projects.html:
    - `h1` with text "Projects"
    - One `p` element: "A selection of projects will be shared here shortly."

    writing.html:
    - `h1` with text "Writing"
    - One `p` element: "Essays and reflections are forthcoming."

    cv.html:
    - `h1` with text "CV"
    - One `p` element: "A downloadable CV will be available here soon."

    404.html:
    - `title` set to "Page Not Found | Emilio [Surname]"
    - `h1` with text "Page not found"
    - One `p` with text "The page you are looking for does not exist."
    - An `a href="/index.html"` with text "Return to home page"

    All files must use root-relative paths (starting with `/`). All filenames must be lowercase. No file should contain relative paths like `../`.
  </action>

  <verify>
    <automated>for f in research.html projects.html writing.html cv.html 404.html; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; grep -q 'id="site-nav"' "$f" || { echo "MISSING site-nav in $f"; exit 1; }; grep -q 'src="/assets/js/nav.js"' "$f" || { echo "MISSING nav.js in $f"; exit 1; }; grep -q 'href="/assets/css/style.css"' "$f" || { echo "MISSING style.css in $f"; exit 1; }; done && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - research.html, projects.html, writing.html, cv.html, 404.html all exist at repo root
    - Every file contains `lang="en"` on the html element
    - Every file contains `meta name="viewport"`
    - Every file contains `link rel="stylesheet" href="/assets/css/style.css"`
    - Every file contains `div id="site-nav"`
    - Every file contains `main class="content"`
    - Every file contains `script src="/assets/js/nav.js"` before closing body
    - Every file has a unique, descriptive `title` tag
    - Every file has a unique `meta name="description"`
    - No file contains relative paths (`../`)
    - No file uses "under construction" or apologetic language
    - 404.html contains a link to `/index.html`
    - Opening http://localhost:8000/research.html (and each other stub) shows the shared nav and a page title
  </acceptance_criteria>

  <done>All 5 pages exist with correct HTML shell, shared nav injection point, root-relative asset paths, and intentional stub content. All nav links resolve without 404 when tested via local HTTP server.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to GitHub Pages and verify live URL</name>
  <what-built>
    The complete Walking Skeleton site: index.html (styled home page with name, bio, email, nav), four stub pages (research, projects, writing, cv), custom 404 page, shared CSS with design tokens, and fetch-inject navigation. All files are ready to push and deploy.

    Before deploying, Claude will:
    1. Check the current git remote to determine the GitHub username and verify the repo name matches `[username].github.io`
    2. If the repo is currently named `research-website` (which it appears to be), flag that it must be renamed to `[username].github.io` for GitHub Pages user site deployment -- this requires human action in GitHub Settings or via `gh repo rename`
    3. Commit all site files to the main branch
    4. Push to origin
    5. Enable GitHub Pages via `gh api` (source: main branch, folder: root) if not already enabled

    If the repo name does not match `[username].github.io`, Claude will pause and ask the user to rename it before proceeding.
  </what-built>
  <how-to-verify>
    1. Open the live URL in an incognito/private browser window: https://[username].github.io
    2. Verify: home page loads with name, subtitle, bio, and email visible
    3. Verify: nav bar shows 5 links (Home, Research, Projects, Writing, CV)
    4. Click each nav link -- all 5 pages load without 404
    5. On the home page, click the email link -- mail client should open
    6. Open browser DevTools, toggle device toolbar, select iPhone SE (375px) -- verify no horizontal scroll, text is readable, nav stacks vertically
    7. Verify the browser shows HTTPS (padlock icon, no mixed content warnings)
    8. Visit https://[username].github.io/nonexistent-page -- verify custom 404 page appears with "Return to home page" link

    Note: GitHub Pages CDN propagation can take 5-15 minutes after the first push. If the site shows a GitHub 404, wait a few minutes and try again.
  </how-to-verify>
  <resume-signal>Type "approved" if the site loads correctly at the live URL, or describe any issues you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to GitHub Pages CDN | Static files served over HTTPS; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Public repo | accept | Repo is intentionally public; contains only the researcher's own public information |
| T-01-02 | Spoofing | GitHub Pages URL | accept | HTTPS provided automatically by GitHub Pages; domain is github.io which is GitHub-controlled |
</threat_model>

<verification>
After deployment:
1. Live URL loads without 404
2. All 5 nav links work across all pages
3. HTTPS active (padlock, no mixed content)
4. Mobile responsive at 375px (no horizontal scroll)
5. Custom 404 page shows for invalid URLs
6. Email mailto link works on home page
</verification>

<success_criteria>
- Site is live at https://[username].github.io
- Home page shows name, role, bio, email above the fold
- All 5 pages load without errors
- Shared nav appears on every page
- Mobile layout works (375px viewport, no horizontal scroll)
- HTTPS confirmed
- Custom 404 page works
</success_criteria>

<output>
Create `.planning/phases/01-foundation-home/01-02-SUMMARY.md` when done
</output>
