---
phase: 01-foundation-home
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/js/nav.js
  - index.html
  - research.html
  - projects.html
  - writing.html
  - cv.html
  - 404.html
autonomous: true
gap_closure: true
requirements:
  - FOUND-03
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "index.html h1 contains 'Emilio Izquierdo' (not '[Surname]')"
    - "index.html p.bio contains the researcher's real bio paragraph (no bracket tokens)"
    - "index.html mailto link href is 'mailto:emilioizq27@gmail.com'"
    - "assets/js/nav.js uses link.pathname === currentPath (pathname comparison, not href)"
    - "assets/js/nav.js has .catch(function error handler"
    - "assets/js/nav.js checks res.ok before calling res.text()"
    - "No '[Surname]' token appears in any HTML file"
    - "No '[field]' token appears in any HTML file"
    - "No '[Institution]' token appears in any HTML file"
    - "email@example.com does not appear in any HTML file"
  artifacts:
    - path: "assets/js/nav.js"
      provides: "Nav loader with pathname comparison, res.ok guard, and catch handler"
      contains: "link.pathname === currentPath"
    - path: "index.html"
      provides: "Home page with real researcher identity"
      contains: "Emilio Izquierdo"
    - path: "research.html"
      provides: "Research stub with real name"
      contains: "Emilio Izquierdo"
    - path: "projects.html"
      provides: "Projects stub with real name"
      contains: "Emilio Izquierdo"
    - path: "writing.html"
      provides: "Writing stub with real name"
      contains: "Emilio Izquierdo"
    - path: "cv.html"
      provides: "CV stub with real name"
      contains: "Emilio Izquierdo"
    - path: "404.html"
      provides: "404 page with real name"
      contains: "Emilio Izquierdo"
  key_links:
    - from: "assets/js/nav.js"
      to: "window.location.pathname"
      via: "pathname comparison with / normalization"
      pattern: "currentPath === '/'"
---

<objective>
Close all 3 verification gaps from 01-VERIFICATION.md that block the Phase 1 goal.

Gap 1 (HOME-01, HOME-02): Replace all placeholder tokens ([Surname], [Institution], [field], email@example.com, bracketed bio) across 6 HTML files with the researcher's real content.

Gap 2 (FOUND-03): Fix nav.js active-state detection — use pathname comparison instead of href comparison so the Home link gets aria-current="page" when visiting the root URL.

Gap 3 (FOUND-03): Add res.ok guard and .catch() error handler to nav.js fetch chain so navigation fails gracefully instead of silently disappearing.

Purpose: Without these fixes, a PI visiting the site sees template scaffolding instead of a researcher's identity, the Home nav link is never highlighted, and a fetch error silently removes navigation from every page.

Output: 7 updated files — all placeholder tokens replaced, nav.js fully fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-home/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix nav.js — pathname comparison and error handling (Gaps 2 and 3)</name>
  <files>assets/js/nav.js</files>
  <read_first>assets/js/nav.js</read_first>
  <action>
Replace the entire contents of assets/js/nav.js with the following fixed version. This addresses two defects:

1. Active-state detection (Gap 2 / FOUND-03): Replace `link.href === window.location.href` with a pathname-based comparison. Normalize the root path `/` to `/index.html` so the Home link matches when GitHub Pages serves the root URL.

2. Fetch error handling (Gap 3 / FOUND-03): Add an `if (!res.ok)` guard that throws before calling `res.text()`, preventing a 404 error page from being injected into the nav. Add a `.catch()` handler that logs the error to console.

The complete replacement content for assets/js/nav.js (19 lines):

(function () {
  var placeholder = document.getElementById('site-nav');
  if (!placeholder) return;

  fetch('/components/nav.html')
    .then(function (res) {
      if (!res.ok) throw new Error('nav fetch failed: ' + res.status);
      return res.text();
    })
    .then(function (html) {
      placeholder.innerHTML = html;

      var currentPath = window.location.pathname;
      if (currentPath === '/') currentPath = '/index.html';
      var links = placeholder.querySelectorAll('a');
      links.forEach(function (link) {
        if (link.pathname === currentPath) {
          link.setAttribute('aria-current', 'page');
        }
      });
    })
    .catch(function (err) {
      console.error('Site navigation failed to load:', err);
    });
}());
  </action>
  <verify>
    <automated>grep -c "link.pathname === currentPath" assets/js/nav.js | grep -q "1" && grep -c "res.ok" assets/js/nav.js | grep -q "1" && grep -c ".catch(function" assets/js/nav.js | grep -q "1" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - assets/js/nav.js contains exactly the string `link.pathname === currentPath` (not `link.href === window.location.href`)
    - assets/js/nav.js contains `if (!res.ok) throw new Error('nav fetch failed: ' + res.status);`
    - assets/js/nav.js contains `.catch(function (err) {`
    - assets/js/nav.js contains `console.error('Site navigation failed to load:', err);`
    - assets/js/nav.js contains `if (currentPath === '/') currentPath = '/index.html';`
    - The old string `link.href === window.location.href` does NOT appear in the file
  </acceptance_criteria>
  <done>nav.js uses pathname comparison with root-path normalization, checks res.ok before parsing response body, and catches fetch errors with a console.error fallback. The old href comparison is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all placeholder tokens in HTML files with real content (Gap 1)</name>
  <files>index.html, research.html, projects.html, writing.html, cv.html, 404.html</files>
  <read_first>index.html, research.html, projects.html, writing.html, cv.html, 404.html</read_first>
  <action>
Replace every placeholder token across all 6 HTML files with the researcher's real content. The exact replacements are:

TOKEN REPLACEMENTS (apply globally across all files):
- `[Surname]` becomes `Izquierdo` (every occurrence in every file)
- `[field]` becomes `psychology and cognitive science` (in meta descriptions)
- `email@example.com` becomes `emilioizq27@gmail.com` (both the href and the visible text in index.html)

FILE-SPECIFIC CHANGES:

**index.html** (the most changes):
- Line 6 title: `Emilio Izquierdo | Researcher`
- Line 7 meta description: `Personal website of Emilio Izquierdo — researcher in psychology and cognitive science. Home, Research, Projects, Writing, and CV.`
- Line 15 h1: `Emilio Izquierdo`
- Line 16 subtitle: `Lab Manager / Research Specialist &mdash; Recent graduate, Case Western Reserve University`
- Lines 17-22 replace the ENTIRE p.bio content (everything between the opening and closing p.bio tags) with exactly this single paragraph:
  `I recently graduated from Case Western Reserve University with degrees in psychology and cognitive science. My research focuses on individual differences — why the same stressor, the same stimulus, the same treatment lands differently across people, and what physiological and cognitive mechanisms explain that variance.`
- Line 24 mailto href: `mailto:emilioizq27@gmail.com`
- Line 24 visible email text: `emilioizq27@gmail.com`

**research.html:**
- Line 6 title: `Research | Emilio Izquierdo`
- Line 7 meta description: `Research interests and past work of Emilio Izquierdo — researcher in psychology and cognitive science.`

**projects.html:**
- Line 6 title: `Projects | Emilio Izquierdo`
- Line 7 meta description: `A selection of projects by Emilio Izquierdo — personal work, tools, and side projects outside of academic research.`

**writing.html:**
- Line 6 title: `Writing | Emilio Izquierdo`
- Line 7 meta description: `Essays and reflections by Emilio Izquierdo.`

**cv.html:**
- Line 6 title: `CV | Emilio Izquierdo`
- Line 7 meta description: `Curriculum vitae of Emilio Izquierdo — researcher in psychology and cognitive science.`

**404.html:**
- Line 6 title: `Page Not Found | Emilio Izquierdo`

After all replacements, verify that ZERO bracket tokens remain by searching all 6 files for `[Surname]`, `[field]`, `[Institution]`, and `email@example.com`. None should be found.
  </action>
  <verify>
    <automated>grep -rl "\[Surname\]\|\[field\]\|\[Institution\]\|email@example.com" index.html research.html projects.html writing.html cv.html 404.html && echo "FAIL — tokens remain" || echo "PASS — no tokens found"</automated>
  </verify>
  <acceptance_criteria>
    - index.html line 6 contains exactly `Emilio Izquierdo | Researcher` in the title tag
    - index.html h1 contains exactly `Emilio Izquierdo`
    - index.html subtitle contains exactly `Lab Manager / Research Specialist &mdash; Recent graduate, Case Western Reserve University`
    - index.html p.bio contains exactly `I recently graduated from Case Western Reserve University with degrees in psychology and cognitive science. My research focuses on individual differences`
    - index.html mailto href is exactly `mailto:emilioizq27@gmail.com`
    - index.html visible email text is exactly `emilioizq27@gmail.com`
    - research.html title contains `Emilio Izquierdo`
    - research.html meta description contains `psychology and cognitive science`
    - projects.html title contains `Emilio Izquierdo`
    - writing.html title contains `Emilio Izquierdo`
    - cv.html title contains `Emilio Izquierdo`
    - cv.html meta description contains `psychology and cognitive science`
    - 404.html title contains `Emilio Izquierdo`
    - grep -r "\[Surname\]" across all 6 HTML files returns zero matches
    - grep -r "\[field\]" across all 6 HTML files returns zero matches
    - grep -r "\[Institution\]" across all 6 HTML files returns zero matches
    - grep -r "email@example.com" across all 6 HTML files returns zero matches
  </acceptance_criteria>
  <done>All 6 HTML files contain the researcher's real name (Izquierdo), real institution (Case Western Reserve University), real bio paragraph, real email (emilioizq27@gmail.com), and real field (psychology and cognitive science). Zero placeholder tokens remain in any HTML file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan modifies static content and fixes existing client-side JavaScript. No user input is processed, no external services are contacted beyond the existing same-origin fetch of nav.html.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | index.html email | accept | Email address is intentionally public (researcher's contact info for PIs). Mailto link is the expected disclosure channel. |
| T-01-SC | Tampering | No package installs | accept | This plan modifies only hand-written HTML and JS files. No npm/pip/cargo installs. No supply chain risk. |
</threat_model>

<verification>
Run these checks after both tasks complete:

1. `grep -r "\[Surname\]" index.html research.html projects.html writing.html cv.html 404.html` returns no matches
2. `grep -r "\[field\]" index.html research.html projects.html writing.html cv.html 404.html` returns no matches
3. `grep -r "email@example.com" index.html` returns no matches
4. `grep -c "link.pathname === currentPath" assets/js/nav.js` returns 1
5. `grep -c "res.ok" assets/js/nav.js` returns 1
6. `grep -c ".catch" assets/js/nav.js` returns 1
7. `grep "Emilio Izquierdo" index.html` returns matches in title and h1
8. `grep "emilioizq27@gmail.com" index.html` returns matches in mailto href and visible text
</verification>

<success_criteria>
All 3 verification gaps from 01-VERIFICATION.md are closed:
- Gap 1: Every HTML file shows real researcher content — no placeholder tokens remain
- Gap 2: nav.js uses pathname comparison so Home link gets aria-current="page" at root URL
- Gap 3: nav.js has res.ok guard and .catch() handler for graceful fetch failure
</success_criteria>

<output>
Create `.planning/phases/01-foundation-home/01-03-SUMMARY.md` when done
</output>
